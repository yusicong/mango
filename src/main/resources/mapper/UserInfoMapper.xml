<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ysc.device.service.repository.UserInfoMapper">
  <resultMap id="BaseResultMap" type="com.ysc.device.service.domain.entities.User">
    <result column="Id" jdbcType="BIGINT" property="Id" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="password" jdbcType="VARCHAR" property="password" />
  </resultMap>

  <select id="findUserById" parameterType="java.lang.String" resultMap="BaseResultMap">
    select
    Id,username,password
    from user_info
    where Id = #{Id,jdbcType=VARCHAR}
  </select>

  <select id="findByUsername" parameterType="java.lang.String" resultMap="BaseResultMap">
    select
    Id,username,password
    from user_info
    where username = #{username,jdbcType=VARCHAR}
  </select>


  <select id="findByPhone" parameterType="java.lang.String" resultType="java.lang.String">
    select
    mobile
    from t_user
    where mobile = #{mobile,jdbcType=VARCHAR}
  </select>
  <insert id="insertUser" parameterType="com.ysc.device.service.domain.entities.RegisterEntity" >
    insert into t_user
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="authType != null">
        auth_type,
      </if>
      <if test="mobile != null">
        mobile,
      </if>
      <if test="password != null">
        password,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="authType != null">
        #{authType},
      </if>
      <if test="mobile != null">
        #{mobile},
      </if>
      <if test="password != null">
        #{password},
      </if>
    </trim>
  </insert>

</mapper>